{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Attach to Chrome",
        "port": 3000,
        "request": "attach",
        "type": "chrome",
        "webRoot": "${workspaceFolder}"
      },
      {
        "type": "pwa-chrome",
        "request": "launch",
        "name": "Debug react",
        "url": "http://localhost:3000",
        "webRoot": "${workspaceFolder}"
      }
    ]
  }